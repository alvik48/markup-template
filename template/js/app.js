'use strict';

var app = angular.module('app', [
    'ngRoute',
    'app.filters',
    'app.services',
    'app.directives',
    'app.controllers'
]);

/**
 * App routes
 */
app.config(function($routeProvider) {

});